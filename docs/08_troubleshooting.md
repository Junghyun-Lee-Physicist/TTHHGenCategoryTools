# 08 — Troubleshooting: 증상 → 원인 → 해결

> **목적**: 개발·운영 중 실제로 발생했던 문제의 전 목록. 같은 문제를 두 번 디버깅하지 않기 위함.
> **대상 독자**: 빌드/실행/CRAB/검증에서 뭔가 깨진 사람.
> **상태**: 살아있는 문서 — 항목은 해결 시마다 추가. 마지막 갱신 2026-07-28 (T-23 압축: condor 7대 함정).
> **관련**: 환경 기인 항목의 일반화는 [09_environment.md](09_environment.md), 원 기록은 [legacy/](legacy/) 동결 문서들.

형식: **T-n | 증상 | 원인 | 해결** (+ 재발 방지가 코드에 어떻게 박혔는지).

## 생산 (GenSidecar / 그 전신)

- **T-1** | cmsRun 콘솔이 `JetPtMismatch`/`MissingJetConstituent`/`NullTransverseMomentum` 경고로 도배 | `prunedGenParticles` 기반 재클러스터링 — miniAOD `slimmedGenJets`는 constituent가 일부 truncate됨 | 재클러스터링 자체를 중단하고 miniAOD의 pre-stored `slimmedGenJetsFlavourInfos` 사용 → 경고 원천 소멸.
- **T-2** | 자릿수 분해에서 10000자리(c-from-W)만 38% 일치 | 직접 재구현이 `cHadFromTopWeakDecay` flag를 단독 신뢰 — 표준 plugin은 mother-chain으로 W ancestor를 재추적 | 재구현 포기, 표준 `categorizeGenTtbar` 호출 (D3) → 전 자릿수 100%.
- **T-3** | `ModuleNotFoundError: ...GenTtbarCategorizer_cfi` / InputTag 불일치 | cfi 파일명은 lowercase `categorizeGenTtbar_cfi`; 출력은 라벨된 `("categorizeGenTtbar","genTtbarId")` | `PhysicsTools.NanoAOD.ttbarCategorization_cff` 통째 import (canonical 경로).
- **T-4** | 10_6_X에서만 터지는 5종: non-ASCII SyntaxError / `Process has no attribute MessageLogger` / MessageLogger 새 category 무시 / `tuple has no attribute find` / `-Werror=unused-variable` | Python 2 + 옛 cmssw의 규약 차이 | PEP 263 헤더, `process.load("FWCore.MessageService.MessageLogger_cfi")`, `categories.append(...)`(hasattr 가드), 명시적 `cms.InputTag(...)`, unused 변수 제거. 일반화 표: [09](09_environment.md) §2.
- **T-5** | hand-written NANO cfg에서 `ProductNotFound`(jetCorrFactorsAK8 → finalBoostedTaus → bitmapVIDForEle)가 **연쇄** 발생, 이후 `unsupported type`의 원인 판별 불능 | customizer/era-modifier 적용 시점·등록 순서를 손으로 재현하는 것의 본질적 fragility | **cmsDriver-emit + inject 패턴**으로 전환 (같은 customizer가 cmsDriver cfg에서는 정상). ttbarId-extend(Approach 3)로 이행하면서 이 failure class 자체가 소멸 (D1). `addColumnValue<int>`의 10_6_X 함정(4번째 인자 `IntColumn` 명시 필요)은 enriched 계열에만 해당 — [10](10_enriched_nanoaod_archive.md).
- **T-6** | TrigReport "Unrunnable schedule" | Sequence `+=`로 task 결합 시도 | `Task.associate()` 사용 (10_6_X scheduling 규약).
- **T-7** | ttbarId-extend tree가 `ttbarIdSidecar/Events`에 생김 → 비교기 default 실패, friend-tree prefix 필요 | TFileService의 모듈-label 디렉토리 격리 | analyzer가 자기 TFile을 열어 top-level `Events` 생성 (D5). 부수: `tree->Write(); file->Write();` 순서가 tree를 **두 번**(cycle 2개) 쓰던 것도 `Write("",kOverwrite)`로 해결.
- **T-8** | 비교기 join이 0 entries | `event` branch를 `uint64_t` 등으로 SetBranchAddress — ROOT는 `ULong64_t`(type-id 'l')를 엄격 구분 | `ULong64_t` 명시. [00_PROMPT](../00_PROMPT.md) §8의 타입 계약으로 고정.
- **T-9** | GenJet η 일치도가 ~86%로 낮음 | NanoAOD `GenJet_eta`는 precision=12 mantissa 양자화(상대 step ~2.44e-4) — 절대 tol 1e-4가 그보다 빡빡 | relative-aware tol `5e-4·max(1,|a|)` → 즉시 100%. (pT는 precision=-1이라 절대 tol 충분.)
- **T-10** | tt+bbb/tt+4b가 **한 번도** 생성 안 됨 (통계 부족으로 오인) | split 조건이 `genTtbarId%100==56`에 결합 — 표준은 56을 절대 만들지 않음 (TWiki Example 2 혼동) | tt4b 950만 event cross-tab으로 확정(56=0건, nAddBJets≥3=188만 건) 후 조건을 `nAddBJets>=3`으로 교체 (D4). 검증 도구도 같은 규칙(iff)을 검사하도록 갱신됨.
- **T-11** | CRAB 제출이 `'year' not registered / Unknown variable`로 즉사 | enriched용 submitter를 복사하며 `pyCfgParams=["year=..."]`가 남음 — ttbarId-extend cfg는 `year` 미등록 | `pyCfgParams=["outputFile=ttbarIDExtend.root"]` + `JobType.outputFiles=["ttbarIDExtend.root"]` 선언. (VarParsing의 `_numEventN` suffix 우려는 이후 7샘플 production 성공으로 실증 해소 — 산출 filelist가 `Validation/filelists/ttbarId-extend/`에 존재.)

## 검증 (Validation)

- **T-12** | 2-key(run,event) map 적재가 duplicate abort — TTToSemiLeptonic에서 15.27M건 | MC event 번호는 lumisection 내에서만 유일 (같은 run·event, 다른 lumi) | 3-key `(run,lumi,event)` (D7). 중복 감지 abort는 설계된 안전장치 — 끄지 말 것. / 대용량 map ~20 GB 초과 → external sort 경로 (D8, T와 별개 성능 항목). / lumi-범위 chunking 아이디어는 `scanOrder` 실측(파일 내부 미정렬 + lumi 범위 중첩)으로 기각.

## 병합·rename 이후 (v11, 예방적 항목)

- **T-13** | (예상) 구 `crab_*` 프로젝트에 `--resubmit`이 실패하거나 엉뚱한 cfg를 집음 | CRAB 프로젝트 디렉토리는 제출 당시의 pset 경로(`.../NanoExtension/...`)를 기억 | 구(pre-v11) 태스크의 resubmit은 구 체크아웃에서 수행; 신규 제출은 새 경로로. `requestName`이 겹치면 `site_config.yaml`의 `request_name_tag`를 bump.
- **T-14** | (예상) `preflight.py`가 "plugin .so missing" | rename 후 stale build area | `scram b clean && scram b -j8` — plugin lib 이름이 `pluginTTHHGenCategoryToolsTtbarIdExtender*`로 재생성되는지 확인 ([01](01_status.md) O1).

## v12 첫 lxplus 빌드 (실측)

- **T-15** | `scram b`가 `Validation/`을 컴파일하려다 `fatal error: TChain.h / TCanvas.h: No such file or directory`로 전량 실패 (2026-07-05 첫 빌드) | scram은 `<Package>/src/*.cc`를 **BuildFile.xml이 없어도** 자동으로 패키지 라이브러리로 컴파일하려 든다. `Validation/src/`가 바로 그 특수 경로에 걸려, scram이 ROOT include 경로(`root-config --cflags`) 없이 컴파일 → ROOT 헤더 못 찾음. (로그의 `Entering library rule at TTHHGenCategoryTools/Validation` + `.../Validation/src/TTHHGenCategoryToolsValidation/*.cc.o`가 증거.) | **소스 디렉토리를 `Validation/src/` → `Validation/tools/`로 rename**하고 Makefile의 `SRCDIR := tools`로 변경. `tools/`는 scram의 자동 컴파일 대상이 아니므로 scram이 완전히 무시하고, standalone `make`만 `tools/*.cc`를 빌드한다. 이것이 [04](04_decisions.md) D14에서 예고한 fallback의 실제 적용. — plugin 패키지(`TtbarIdExtender/`)는 정상 컴파일됐으므로 subsystem/패키지/plugin-lib 이름 자체는 문제 없음이 이 빌드로 확인됨.
- **T-15 부수**: 같은 빌드에서 `-Wcomment`(multi-line comment) 경고가 여러 도구에서 발생 — `//` 주석 줄 끝의 `\`(예시 명령 줄바꿈)를 컴파일러가 comment continuation으로 해석. 무해하나, 주석 예시에서 줄 끝 `\`를 제거해 정리함.
- **T-16** | 첫 CRAB 제출이 `Cannot find CMSSW configuration file .../run_ttbarIdExtend_cfg.py`로 전량 실패, 이후 재제출은 `[skip] project dir already exists` / `--status`·`--resubmit`은 `Cannot find .requestcache file` | (1) `psetName`이 CWD-상대 경로여서 `src/`가 아닌 데서 실행 시 못 찾음(v12.2에서 절대경로로 수정). (2) 실패한 제출도 `crab_projects/crab_<req>/` 껍데기(`crab.log`·`inputs`·`results`만, `.requestcache` 없음)를 만들고, submit은 이 디렉토리가 있으면 skip, status/resubmit은 `.requestcache`가 없어 실패 | psetName 절대경로 수정본으로 교체 후: **빈 프로젝트 디렉토리 삭제** `rm -rf crab_projects/crab_*_2017_extend` → `source /cvmfs/cms.cern.ch/common/crab-setup.sh`(세션당 1회, `CRABClient` 로드) → `python3 crab/submit_ttbarIdExtend.py --process TT4b --max-files 5`로 스모크. `.requestcache`가 생겨야 제출 성공 = 이후 `--status`/`--resubmit` 동작.
- **T-17** | CRAB `--status`가 전 태스크 `Status on the CRAB server: SUBMITREFUSED` + `Permission denied ... write check at destination site fails` + `gfal-copy ... davs://eoscms.cern.ch:443/eos/cms/store/user/... HTTP 403 : Permission refused` | `storage_site: T2_CH_CERN` + `out_lfn_base: /store/user/...` 조합이 목적지를 **CMS 실험 EOS** `/eos/cms/store/user/`로 보냈고, 그 영역은 별도 CMS-EOS 쓰기 활성화가 필요해 stageout 사전 write-check가 403으로 거부됨. (task는 CRAB 서버까지 등록됐으나 grid로 안 나감 = SUBMITREFUSED. `--status`가 이 정보를 정상 조회한 것이므로 `[status ok]`는 맞음.) | lxplus **개인 EOS**(`/eos/user/<u>/<user>/`)로 보내려면 `storage_site: T3_CH_CERNBOX` 로 바꾼다(v12.4). 제출 전 `crab checkwrite --site=T3_CH_CERNBOX --lfn=/store/user/<user>`로 권한 확인. SUBMITREFUSED된 task의 프로젝트 디렉토리(`crab_projects/crab_*`)는 재제출 전에 지운다(`rm -rf crab_projects/crab_*_2017_extend`) — 안 지우면 submit이 skip. CMS EOS(`/eos/cms/...`)가 꼭 필요하면 `crab checkwrite`로 확인 후 활성화를 CMS computing에 요청.

## UL18 캠페인 운영 (실측, 2026-07-27)

- **T-22** | 검증을 인터랙티브로 돌리다 터미널 3개를 손으로 관리하게 됐고, 그 와중에 AFS quota(10 GB 중 **94%**, `<<WARNING`)로 `SysError in <TFile::Flush> ... (Input/output error)` 가 두 번 났다. 남은 대형 3샘플은 직렬 **~38시간** (2026-07-28) | **작업 규모를 잘못 봤다.** 소형 4샘플을 "수 분"으로 추정해 대화형으로 시작했으나 실측은 샘플당 **11.5분·29분**이었고, 대형은 nano event 수에 비례해 ~6·13·19시간이다. 병목은 CPU 가 아니라(실측 **CPU 효율 25%**) 중앙 NanoAOD **~1.2 TB WAN 읽기** 다. 게다가 산출물을 AFS 홈에 쓰고 있었는데, AFS 는 quota 제한이 있고 토큰이 ~25시간이라 **작업 시간보다 짧다** — 6~19시간짜리 job 에서는 우연이 아니라 **반드시 재발**할 조건이었다. | **`scripts/submit_validation_condor.py` + `scripts/aggregate_validation.py` 신설** (README §4.0). **① 파이프라인 통일** — 소형·대형 구분 없이 전부 `sortSplitExtend` → `matchTtbarIdSorted`. 정렬은 싸고(146 M rows **10분54초**, peak RSS 881 MB 실측) 한 경로로 통일되며 job 당 메모리가 **~16 MB** 로 고정된다. **② job = nano chunk 1개** — `make_nano_filelists_das.sh` 가 이미 만들어 둔 per-job split 을 그대로 소비(대형 45 + 소형 4 chunk). `--sorted-dir` 는 읽기 전용이라 공유 안전. 38시간 → **1~2시간**. **③ 산출물 EOS 강제** — `--out-base` 가 `/afs/` 로 시작하면 submitter 가 FATAL 로 거부한다. **④ 합산기** — chunk 별 `--json` 카운터(매처에 신설)를 합쳐 샘플별 PASS/FAIL 한 장을 낸다. 판정 기준에 **`sum(nano_entries) == DAS nevents`** 와 **`all chunks present`** 를 넣은 것이 핵심 — `unmatched` 는 데이터 손실에 단조 감소라 그것만으로는 완결성을 증명할 수 없다(T-21). **⑤ 스모크 우선** — `--smoke` 가 1샘플 1 chunk 만 제출해 job 로그 한 장을 먼저 보게 한다. stub 로 검증: JSON 스키마 22 key, 실패 chunk 도 JSON 을 남김, 그리고 **`unmatched 0` + 전 chunk 정상인데 chunk 1개 누락 → DAS 대조가 FAIL 을 잡아내는 것**을 합성 fixture 로 확인. **⑥ 실행 환경 분리 (첫 `--preflight` 이 잡아낸 것)** — `condor_submit` 은 **cmssw-el7 안에 없고** EL9 호스트에만 있다. 그런데 `Validation/bin/*` 는 그 컨테이너에서 빌드된 slc7/ROOT 6.14 바이너리라 EL9 워커에서 못 돈다. 즉 **제출은 호스트, 실행은 EL7 컨테이너**다. 따라서 `getenv = True` 를 쓰면 안 된다(EL9 환경을 EL7 payload 에 물리는 것) → submit 파일이 `MY.SingularityImage` 로 EL7 이미지를 요청하고, job 스크립트가 cvmfs 에서 `cmsset_default.sh` → `scramv1 runtime -sh` 로 직접 세팅한다. 바이너리는 **transfer 한다** — 워커에 이 홈의 AFS 토큰이 없어 AFS 읽기는 실패할 것이기 때문. job 스크립트에 단계별 가드를 넣어 원인이 바로 드러나게 했다(실측: 바이너리 없음 **127** / sorted `index.txt` 없음 **125** / chunk 없음 **126**). sorted part 를 EOS POSIX 로 읽는 것은 워커에 대한 **가정**이라 preflight 가 WARN 으로 명시하고 job 이 125 로 분명히 실패하게 했다(`index.txt` 는 `ifstream` 이라 XRootD URL 로 대체 불가). **⑧ `/eos/user` → `/eos/home-j` 재작성 (내가 넣은 버그)** — 절대경로화를 `Path.resolve()` 로 했더니 심볼릭 링크를 따라가 `sorted base` 가 `/eos/home-j/junghyun/...` 로 바뀌어 생성 파일에 박혔다. `/eos/user/...` 가 문서화된 안정 경로이고 워커가 마운트하는 이름이며, `/eos/home-j/...` 는 내부 실현 경로라 워커에 그 이름이 없을 수 있다 → 49 job 전멸. `os.path.abspath()` 로 교체(심볼릭 링크를 따라가지 않고 절대화만 한다). **교훈: 사용자가 준 경로 표기는 보존한다** — 정규화가 곧 개선은 아니다. **⑦ proxy 탐지 버그** — preflight 가 `$X509_USER_PROXY` 만 봐서, `voms-proxy-init` 로 정상 발급된 세션을 "proxy 없음"으로 FAIL 했다. `voms-proxy-init` 은 그 변수를 export 하지 않고 `/tmp/x509up_u<uid>` 에 쓴다 → 그 관례 경로까지 보도록 수정. ⚠️ condor 제출 자체는 여기(ROOT/condor 없는 환경)서 검증 불가 — **`--preflight` → `--smoke` 순서로 실기기 확인 후 전량**. | 
## HTCondor (CERN lxplus batch) — 반드시 읽을 7대 함정

- **T-23** | HTCondor 검증 캠페인을 세우는 동안 **제출 거부 1회 + HOLD 2회 + 조용한 결과 손실 1회**가 났다. 개별 사건보다 **재발 조건**이 중요하므로 규칙으로 압축한다. 원 사건 기록은 [03_changelog](03_changelog.md) v13.16–v13.23 (2026-07-28) | 여섯 가지가 **각각 독립적으로** job 을 죽인다. 다섯째는 실패를 *은폐*해서 나머지 진단을 방해했다 | **CERN HTCondor 7대 함정 — 새 submitter 를 쓸 때 이 표를 먼저 확인한다**<br><br>**① submit 파일에 `/eos` 경로 금지.** 표준 schedd 가 제출 자체를 거부한다(`Standard batch schedds cannot use /eos paths directly within the submit file`). `executable`·`output`·`error`·`log`·`transfer_*`·`queue ... from` 은 **전부 AFS**. 런타임에 job 이 EOS 를 POSIX 로 읽는 것은 별개이고 **허용된다**(워커 가시성 실측 확인). 방지: 생성된 `.sub` 를 `(^\|[^:/])/eos/` 로 자기검사 → 있으면 제출 전 FATAL.<br><br>**② proxy 를 `/tmp` 에 두면 안 된다.** `/tmp` 은 노드 로컬이고, 파일을 실어 보내는 것은 제출 노드가 아니라 **access point(schedd)** 다 → `Transfer input files failure ... /tmp/x509up_uNNN: (errno 2)` 로 HOLD. `voms-proxy-init` 의 기본 출력 위치가 하필 `/tmp` 다. 규칙: `--out $PWD/proxy.cert` + `export X509_USER_PROXY`. 방지: 없거나 `/tmp` 거나 수명 <1 h 이면 제출 거부. **자동 복사는 하지 않는다**(자격증명을 사용자 모르게 홈에 남기지 않는다).<br><br>**③ job 스크립트에 `set -u` 금지.** `cmsset_default.sh` → `cmsset_local.sh` 가 `$CVS_RSH` 를 미설정 참조하므로 `-u` 에서 **첫 setup 줄에 즉사**한다(`CVS_RSH: unbound variable`). 서드파티 셸을 source 하는 스크립트에는 `-u` 를 걸지 않는다. `set -o pipefail` 만. 그리고 세팅 성공을 **검증**한다 — `command -v root-config` 실패 시 exit 123.<br><br>**④ `transfer_output_files` 를 비운다(`""`)**, job 이 `xrdfs mkdir -p` + `xrdcp -f` 로 스스로 EOS 에 올린다. **이것이 다섯째 함정의 해법이다** → ⑤ 참조. `TopCPVGenCategorizer/condor` 가 이미 이 패턴이었다.<br><br>**⑤ [은폐형] 실패가 다른 원인으로 위장된다.** `transfer_output_files` 가 비어 있지 않으면, payload 가 죽어 파일이 없을 때 condor 가 **HOLD 하고 hold 이유로 *transfer 에러*를 보고**한다 → `.err` 한 줄에 있던 진짜 원인(③)이 보이지 않는다. 같은 이유로 출력 보장은 스크립트 **꼬리가 아니라 `trap ... EXIT`** 여야 한다(조기 abort 시 꼬리는 실행되지 않는다). 실패 시 `job_failed:true` 스텁 JSON 을 남겨 합산기가 **FAILED 와 누락을 구분**하게 한다.<br><br>**⑦ [은폐형] 안전장치가 조용히 꺼진 채 PASS 가 난다.** 합산기의 `nano total == DAS nevents` 는 완결성의 **유일한 진짜 증명**인데(⑤ 아래 T-21), `samples_<era>UL.json` 을 못 찾으면 그 기준이 **SKIP 되고 샘플은 그대로 PASS** 했다. 원인은 기본 경로 추정이 `TTHHGenCategoryTools` 와 `tempTTHH` 가 **같은 CMSSW 릴리스**에 있다고 가정한 것 — lxplus 는 각각 10_6_32_patch1 / 14_2_1 이다. 게다가 lxplus 에는 **tempTTHH 가 아예 체크아웃돼 있지 않았다** — 즉 경로를 고치는 문제가 아니었다. 수정: **① 기준 데이터를 이 repo 로 가져왔다** — `Validation/data/das_nevents_<era>.json`(2017·2018, 커밋됨, Validation 짧은 이름으로 키잉). **검증 도구는 자기 기준 데이터를 들고 있어야 한다**; 다른 repo 에 런타임 의존하는 것이 애초의 잘못이었다. tempTTHH 경로는 편의상 fallback 으로만 남겼다. **② 못 찾으면 FAIL** (SKIP 은 `--no-das-check` 를 명시할 때만), 시도한 경로 전부 출력. 부수로 잡은 것: **명시한 `--xsec-db` 가 없을 때 다른 후보로 조용히 대체**하던 것도 금지했다 — 사용자가 지정한 파일을 말없이 바꾸는 것은 명확한 에러보다 나쁘다. **일반 규칙: 안전장치는 입력이 없으면 통과가 아니라 실패해야 한다.**<br><br>**⑥ XRootD 는 host 뒤 슬래시 2개.** `root://eosuser.cern.ch//eos/...` — 1개면 `xrdcp rc=54`. **이 실수는 두 번 했다**: 한 번 잡고 단위검증까지 했는데 코드를 재구성하며 `rstrip("/")` 로 되살렸다. 교훈은 슬래시가 아니라 **검증한 불변식을 사람 기억이 아니라 코드에 고정하라**는 것 → 단정문으로 박았다. 부수: xrdcp 재시도 3회 + 영구 실패 시 exit 122(정상 계산된 chunk 를 EOS 하나로 잃지 않는다).
- **T-21** | `matchTtbarId` 가 `matched 628000 / unmatched 0 / disagree 0 / ALL ... consistent` 로 **통과**를 찍었는데 그 샘플(`ttbb_2L2Nu`)의 nano event 수는 4,792,850 이다 — **13% 만 검사하고 합격 도장** (2026-07-27) | nano 6개 중 5개가 XRootD `[3014] ... Network is unreachable` 로 안 열렸다. **`TChain::Add()` 는 파일을 열지 않는다**(경로만 등록) → `added to chain = 6` 이 찍혀도 실제 entries 는 열린 1개분뿐이었고 도구는 남은 것만으로 진행했다. **결정적으로 나쁜 점**: `unmatched` 는 *nano 에 있는데 extend 에 없는* event 수다. nano 를 잃으면 `unmatched` 는 **작아질 수만 있다** → 데이터가 사라질수록 결과가 좋아 보인다. 완결성 지표가 데이터 손실에 대해 **단조 감소**라, 가장 위험한 green light. (extend 쪽은 정상: unique keys 4,858,850 = MiniAOD 전량.) | **원인은 환경이 아니라 일시적 AAA 장애였다 (2026-07-28 확정).** 같은 파일·같은 컨테이너·같은 4.8.5 클라이언트로 몇 분 뒤 **정상 open**(global redirector / `XRD_NETWORKSTACK=IPv4` / INFN redirector 3가지 모두 OPEN OK, 528,000 entries). replica 9곳 전부 정상이고 `xrdfs stat` 도 처음부터 성공했다 — 실패는 `open` 단계의 일시적 사건이었다. **헛다리였던 단서**: 로그의 `Unable to load sec.protocol plugin libXrdSecztn.so` 는 4.8.5 클라이언트가 ztn(토큰) 을 광고하는 서버를 만날 때 **성공하든 실패하든** 찍히는 무해한 경고다 — 이걸 '낡은 클라이언트 vs 현대 스토리지' 로 오독해 진단이 한 라운드 샜다. WAN 읽기에서 일시 장애는 **정상적으로 발생하는 일**이므로, 해법은 환경 교체가 아니라 **도구의 내구성**이다. **조치 (`matchTtbarId`·`matchTtbarIdSorted` 양쪽)**: **① 재시도** — `assertAllFilesOpen()` 이 체인 구성 **전에** 전 파일을 실제로 열고 tree 존재까지 확인하며, 실패 시 최대 3회·0/5/10초 backoff 로 재시도한다(일시 장애는 자동 복구, 로그에 `retry n/3` 로 남는다). **② 실패 시 hard stop** — 재시도 후에도 남으면 **exit 4**. `--allow-missing-files` 로만 우회 가능하고 그 결과는 **참고용이며 완결성 증명이 아니다**. **③ 사후 truncation 검사** — `assertChainComplete()` 가 `TChain::GetEntries()` 를 open-check 합계와 대조한다. check 통과 후 읽기 도중 파일이 죽는 경로까지 막는다. **④ 커버리지 상시 기록** — `open-check N/M files OK, total entries = ...` 가 항상 찍히므로 로그만 봐도 몇 %를 검사했는지 안다. stub ROOT 로 4가지(정상 / 일시장애 복구 / 영구실패 exit 4 / 사후 truncation exit 4) 동작 검증. **교훈**: 완결성 지표가 데이터 손실에 대해 단조 감소하면 그 지표만으로 합격 판정하면 안 된다 — 입력이 전량 읽혔는지를 **따로** 강제해야 한다.
- **T-20** | "이 캠페인이 실제로 어떤 설정으로 돌았나" 를 알 수가 없었다. 2026-07-27 18:50 재제출 후 `--report` 의 job 수가 6샘플은 upj=1, `TTbar_SemiLep` 만 upj=10 패턴이었는데, 워킹트리의 `site_config.yaml` 은 이미 upj=10 이라 설명이 안 됐다. 원인 규명에 3라운드가 걸렸다. | **제출 로그에 유효 설정이 안 찍혔다.** `submit_one()` 은 requestName/dataset/storage/pyCfgParams 만 출력했고 `unitsPerJob`·`maxMemoryMB` 는 안 찍었다. 유일한 증거가 `crab_projects/*/crab.log` 안의 `config.Data.unitsPerJob` 이었다. 부수적으로 **timezone 함정**: CRAB task timestamp 는 **UTC** (`260727_165031`)인데 우리 로그 파일명은 로컬(CEST=UTC+2, `submit_..._1850.log`)이라 같은 제출이 다른 시각으로 보였고, 그것 때문에 "옛 task 를 재활용한 것 아닌가"라는 잘못된 가설을 세웠다. | **① 확정된 사실(증거)**: 7 task 전부 `260727_1650xx` UTC(=18:50 로컬) 20초 내 신규 제출, `[skip] already exists` 0줄, `crab.log` 에 `unitsPerJob = 1`(6샘플)/`10`(SemiLep) + `maxMemoryMB = 2000`. `maxMemoryMB` 가 2000 인 것이 결정적 — 2500 은 upj=10 기본값과 **같은 커밋**(v13.12)이므로, 제출 시점 체크아웃에 `datasets.yaml`(v13.11, SemiLep per-entry)만 있고 `site_config.yaml`(v13.12)은 없었음이 증명된다. **② 재발 방지**: `submit_one()` 이 이제 `splitting / unitsPerJob (+ 어디서 온 값인지) / maxMemoryMB / maxJobRuntimeMin` 을 태스크마다 출력한다 → tee 한 제출 로그만으로 답이 나온다. `units_per_job` 출처는 `datasets.yaml per-entry` / `site_config resources.extend` / `hardcoded fallback (1) -- site_config key MISSING` 중 하나로 명시(마지막 경우가 곧 설정 사고 경보다). **③ 판정 규칙**: 돌고 있는·끝난 캠페인의 설정은 **현재 워킹트리로 추정하지 말고** `--report` job 수와 `crab.log` 로 판정한다. 워킹트리는 *다음* 제출을 말할 뿐이다. **④ 결과적으로 무해**: 산출물은 packing 과 무관하게 동일(D15), failed 0, 샘플당 timestamp 1개. mem 2000 으로도 전량 완주했다(관측 피크 1731 MB) — 2500 은 다음 제출부터. 재생산하지 않는다.
- **T-19** | 2018 `TTbar_SemiLep` 이 제출 직후부터 **하루 동안 아무 job 도 안 돌았다.** `--resubmit` 은 `Status information is unavailable`, `--status` 는 `Status on the CRAB server: SUBMITREFUSED` + `Warning: The splitting on your task generated 10010 jobs. The maximum number of jobs in each task is 10000` (2026-07-27) | **CRAB 는 task 당 job 10,000 개가 하드 상한이다.** 이 MiniAOD 는 **10,010 파일**이고 `units_per_job: 1` 이었으므로 10,010 job → 10 개 초과. 치명적인 점은 **제출 시점에 거부되지 않는다**는 것: client 는 `submitted : 7` 로 성공을 보고하고, **서버가 나중에** task 를 `SUBMITREFUSED` 로 세워 둔다. 그래서 (a) 제출 로그·문서에 "7 tasks / 20,953 jobs 제출됨"이 남았지만 실제로 돈 것은 **6 tasks**, (b) `jobsPerStatus` 가 비어 있어 `--report` 행이 전부 0 → "아직 안 시작"과 구분이 안 되고, (c) `crab resubmit` 은 **scheduler 에 도달한 task 의 FAILED job 만** 재큐하므로 원리적으로 손을 못 쓴다. 20,953 = 188+**10,010**+7,195+3,069+219+169+103 이라 총합만 보면 이상이 안 보였다. | **① 예방**: `--preflight --check-das` 가 이제 DAS 의 `nfiles` 를 읽어 `ceil(nfiles/units_per_job)` 를 계산하고 10,000 초과면 **FAIL**(90% 초과면 WARN), 필요한 `units_per_job` 값까지 알려 준다. **② 수정 (최종)**: **2018 전량 재제출 + 캠페인 기본값 변경** — `site_config.yaml` `resources.extend.units_per_job` **1 → 10**, `max_memory_mb` **2000 → 2500**. **실측 재제출 결과: 7 tasks / 11,946 jobs / failed 0, 전부 COMPLETED** — 다만 재제출 시점이 site_config 기본값 상향 **전**이어서 설정이 혼합이었다(`TTbar_SemiLep` 만 per-entry override 로 upj=10 → **1,003** jobs = 1,001 + CRAB block 경계 +2; 나머지 6개는 upj=1). 무해하므로 재생산하지 않았다. '전부 upj=10 = 2,097 jobs' 는 다음 제출의 기대치다 (v13.14). `datasets.yaml` 의 `TTbar_SemiLep` per-entry `units_per_job: 10` 은 기본값과 같아졌지만 **floor 로 남겨 뒀다**(이 dataset 만 1 이 불가능하므로 전역 기본값이 되돌려져도 살아남게). 항목별 override 는 원래 지원됐다(`build_config()`: `dataset_entry.get("units_per_job", res.get(...))`). 최소 수정은 2(5,005 jobs)였으나 기각 — job 2.4분 중 ~90초가 startup 이라 upj=1 은 CRAB 의 `CPU efficiency < 50%` 경고(실측 20~45%, waste 50~58%)의 원인 그 자체였고, upj=10 이면 오버헤드 비중 63%→14% 다. 묶음 단위는 **물리에 무관**하다(`matchTtbarId` 가 filelist 전체에 3-key map 하나를 만들 뿐, 어느 파일에서 온 행인지 안 본다). 규칙의 정본은 [04](04_decisions.md) **D15**. **③ 가시성**: `--report`/`--status` 가 task-level `SUBMITREFUSED`/`SUBMITFAILED`/`FAILED`/`UNKNOWN` 을 `[!! ... !!] this task will NEVER run` 로 크게 찍고, 요약에 DEAD TASK 목록을 낸다. **④ 복구 절차**: 이 사고 하나만 고칠 거라면 SUBMITREFUSED 는 산출물이 없으므로 EOS 청소가 불필요하다 — `rm -rf crab_projects/crab_TTbar_SemiLep_2018_extend` 후 `--process TTbar_SemiLep --era 2018` 로 재제출(`--kill` 도 불필요: 죽일 job 이 없다). **다만 실제로 택한 길은 전량 재제출이다**(②) — 그 경우 순서가 중요하다: kill → `--report` 의 run/idle/transf 가 **전부 0 이 될 때까지 대기** → EOS `2018/` 삭제 → project dir 삭제 → preflight → 제출. kill 완료 전에 EOS 를 지우면 늦은 stage-out 이 지운 디렉토리를 되살려 timestamp 2개 → `matchTtbarId` exit 7 이 된다. 전체 절차는 `TtbarIdExtender/README.md` **§2.2c**. — **T-17 과 구별**: 둘 다 SUBMITREFUSED 지만 T-17 은 stage-out 권한(403), 이건 job 수 초과다. `--status` 의 CRAB 원문 경고 줄로 갈린다.
- **T-18** | `--resubmit` 이 `resubmitted : 3` 이라고 했는데 실제로 재제출된 건 **2개**였다 (2026-07-27, UL18 진행 중) | `crabCommand("resubmit", ...)` 의 결과 통보가 일관되지 않다. 세 가지가 두 라벨로 뭉개졌다: ① `Found no jobs to resubmit. Only jobs in status failed can be resubmitted.` → **예외를 던진다** 그래서 `[resubmit FAILED]` 로 찍혔는데 이건 **실패한 job 이 없다는 좋은 소식**이다. ② `The task has not been submitted to the Grid scheduler yet ... will not proceed with the resubmission.` → **예외를 안 던진다.** CRAB 이 stdout 에 거부 사유만 찍고 정상 반환하므로 코드가 `[resubmit ok]` 를 찍고 **성공으로 집계**했다 — 아무것도 재큐되지 않았는데 "재제출됨"으로 보이는 게 위험한 쪽이다. ③ `Resubmit request sent to the server.` → 유일한 진짜 성공. | `crab_action_one()` 이 CRAB 의 stdout/stderr 를 캡처해 그대로 되돌려 찍고(`    | ` 접두사), `classify_resubmit()` 으로 **sent / nothing / refused / unclear / error** 로 분류한다. 집계는 **`sent` 만** 재제출로 센다. 요약 끝에 outcome breakdown 과 `NOTE: 'refused' tasks were NOT resubmitted` 를 찍는다. 사용자 로그의 5가지 문자열로 분류기 회귀 테스트 통과(v13.9). `refused` 는 몇 분 뒤 `--resubmit` 재실행하면 된다 — task 가 CRAB 서버엔 있지만 아직 grid scheduler 로 안 나간 과도 상태다.
